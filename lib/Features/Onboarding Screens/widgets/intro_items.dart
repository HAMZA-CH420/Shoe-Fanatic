import 'package:flutter/material.dart';

class IntroItems {
  IntroItems({
    required this.image,
    required this.description,
    required this.banner,
  });

  final Widget description, banner, image;
}

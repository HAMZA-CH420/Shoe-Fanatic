import 'package:flutter/material.dart';

class Sold extends StatelessWidget {
  const Sold({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Sold"));
  }
}
